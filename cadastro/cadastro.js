/*
function handleResize() {
    //var alturaDaDiv = document.querySelector("#lateral").clientHeight;

    if(window.clientWidth >= 1451){
        document.querySelector("#lateral").clientHeight = 1000;
        console.log('tudo certo2');
    }

    console.log('tudo certo1');
    //window.clientHeight;
}


window.addEventListener('resize', handleResize);

*/